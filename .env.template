# REQUIRED variables
# Create a new .env file, copy the contents below, then replace the following values with your specific values.
NEXT_PUBLIC_API_URL=<VALUE>
NEXT_PUBLIC_SOCKET_URL=<VALUE>
NEXT_PUBLIC_DEFAULT_TOPIC_ID=<VALUE>
# SESSION_SECRET must be a string of at least 32 characters (256 bits)
# Used for encrypting session cookies with A128CBC-HS256 algorithm
# Generate a secure random string, e.g.: openssl rand -base64 32
SESSION_SECRET=<VALUE>

# Footer links
NEXT_PUBLIC_ABOUT_URL=<VALUE>

# Optional variables

